import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Watchuserpage2Page } from './watchuserpage2.page';

describe('Watchuserpage2Page', () => {
  let component: Watchuserpage2Page;
  let fixture: ComponentFixture<Watchuserpage2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Watchuserpage2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
